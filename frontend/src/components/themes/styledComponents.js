import styled from "styled-components";

export const ThemeSelector = styled.form`
  label {
    min-width: 250px;
    width: 100%;
  }
  select {
    border: none;
    color: black;
    border-radius: 5px;
  }
`;
