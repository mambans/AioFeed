import styled from "styled-components";

const ThemeSelector = styled.form`
  label {
    min-width: 250px;
  }
  select {
    border: none;
    color: black;
    border-radius: 5px;
  }
`;

export { ThemeSelector };
